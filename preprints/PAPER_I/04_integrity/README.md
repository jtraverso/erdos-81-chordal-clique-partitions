# Integrity

`SHA256SUMS.txt` provides file-level integrity checks for the public release.
`RELEASE_MANIFEST.json` records file paths, sizes, and hashes.
`PROVENANCE.md` identifies the authoritative inputs.

These artifacts detect accidental or unauthorized file changes. They do not by
themselves establish mathematical correctness.
